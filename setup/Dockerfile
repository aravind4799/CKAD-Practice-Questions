# Use a lightweight base image
FROM alpine:3.19

# Set a working directory inside the container
WORKDIR /app

# Run a simple command when the container starts
CMD ["echo", "Container image built successfully!"]
